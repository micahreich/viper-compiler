(record node ((val int) (next node)))

(fun random ((head node)) bool
    (if
        (= (lookup head val) 0)
        (false)
        (true) 
    )
)


(let
    ((head (node 0 (node 1 NULL))))
    (random head)
)