(record bst_node ((val int) (left bst_node) (right bst_node)))

(fun adder ((val int) (accum int)) int
    (if
        (= val 0)
        accum
        (adder (sub1 val) (+ accum val))
    )
)

(let ((res (adder 5 0)))
    res
)